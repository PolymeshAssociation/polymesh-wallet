import styled from 'styled-components';

import { Button } from '../Button';

export const ButtonSmall = styled(Button)({
  lineHeight: '1rem',
  maxHeight: '2rem',
  minWidth: '80px',
  padding: '0.5rem 1rem'
});
