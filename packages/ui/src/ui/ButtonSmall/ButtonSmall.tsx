import styled from 'styled-components';
import { Button } from '../Button';

export const ButtonSmall = styled(Button)({
  padding: '0.5rem 1rem',
  minWidth: '80px',
  minHeight: '2rem',
  lineHeight: '1rem'
});
